
import { add, multiply } from './utils/math';
import { concatenate } from './utils/string';

console.log(`2 + 3 = ${add(2, 3)}`);
console.log(`2 * 3 = ${multiply(2, 3)}`);
console.log(`Concatenated string: ${concatenate('Hello, ', 'SWC!')}`);
